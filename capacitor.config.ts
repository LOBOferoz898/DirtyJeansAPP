import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'DirtyJeans-proyect-ionic',
  webDir: 'www'
};

export default config;
